package Menus.Cammands.BattleCommands;

import Menus.Cammands.Command;
import Menus.MenuManager;

public class ShowCollectableInfo extends Command {
    public ShowCollectableInfo(){
        super("(show|Show) info");
    }

    public void execute(){
//        view.show(MenuManager.getCurrentMatch().getSelectedCollectable().toString());
        // todo ought to be designed by abolfazl
    }
}
