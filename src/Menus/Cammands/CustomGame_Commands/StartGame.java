package Menus.Cammands.CustomGame_Commands;

import Menus.Cammands.Command;

public class StartGame extends Command {

    public StartGame(){
        super("start game (//w+) (kill hero|collect flag|hold flag) (//d+)");
    }

    public void execute(){
        // todo
    }
}
