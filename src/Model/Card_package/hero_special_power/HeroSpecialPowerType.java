package Model.Card_package.hero_special_power;

public enum  HeroSpecialPowerType {
    BUFFS, EFFECTS, CELL_EFFECT   // this type have arrayList
     //these types have class
    //these types would handle by type
}
