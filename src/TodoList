

todo :
 -- current account is not saved in a file or something so better handled before deadline
 -- (show)s in collection and shop : show shop cards, showCollection(shop), show all decks, show cards , show deck(Shop)
 -- define collectable interface for collectable items and flag
 -- define buyable interface for some units (forces and usable items)


------------------------------------------------------------------------------------------------------------------------
Abolfazl todo list:

-->> TODO COLLECTABLE CARD etc ToString methods ought to be HANDLED .
